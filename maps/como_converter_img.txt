para converter imagem em PNG em xpm

usar o comando:

para instalar o magick:

wget https://imagemagick.org/archive/binaries/magick

verificar se ja tem instalado:
./magick -version

comando de conversão:
./magick imagem.png -resize 64x64 -alpha off imagem.xpm

